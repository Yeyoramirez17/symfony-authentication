<nav class="flex items-center justify-between flex-wrap bg-gray-800 p-6">
    <div class="flex items-center flex-shrink-0 text-white mr-6">
        <a href="{{ path('app_default') }}">
            <span class="font-semibold text-3xl tracking-tight">Symfony Authentication</span>
        </a>
    </div>
    <div class="block lg:hidden">
        <button class="flex items-center px-3 py-2 border rounded text-gray-200 border-gray-400 hover:text-white hover:border-white">
            <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
        </button>
    </div>
    <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
        <div class="text-sm lg:flex-grow">

        </div>
        <div>
            {% if app.user %}
                <div class="flex flex-row items-center">
                    <a
                            href="{{ path('app_logout') }}"
                            class="px-4 py-2 mt-2 font-medium text-gray-700 transition-colors duration-200 transform rounded-md dark:text-gray-200 md:mt-0 hover:bg-gray-100 dark:hover:bg-gray-700"
                    >
                        Logout
                    </a>
                </div>
            {% else %}
                <div class="flex flex-row items-center">
                    <a
                            href="{{ path('app_login') }}"
                            class="{{ (app.request.attributes.get('_route') == 'app_login') ? 'border-2 ' : '' }}px-4 py-2 mt-2 font-medium text-gray-700 transition-colors duration-200 transform rounded-md dark:text-gray-200 md:mt-0 hover:bg-gray-100 dark:hover:bg-gray-700"
                    >
                        Login
                    </a>
                    <a
                            href="{{ path('app_register') }}"
                            class="{{ (app.request.attributes.get('_route') == 'app_register') ? 'border-2 ' : '' }}px-4 py-2 mt-2 font-medium text-gray-700 transition-colors duration-200 transform rounded-md dark:text-gray-200 md:mt-0 hover:bg-gray-100 dark:hover:bg-gray-700"
                    >
                        Register
                    </a>
                </div>
            {% endif %}
        </div>
    </div>
</nav>
